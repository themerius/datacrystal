# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Cloned third-party reference repos (see README)
resources/

# Test/demo artifacts
.pytest_cache/
.ruff_cache/
.hypothesis/
.benchmarks/
*.store/
# a store born from a typo'd Store.open("memory:") — the in-memory sentinel is :memory:
memory:/

# OS noise
.DS_Store

# agent worktrees (transient, isolation runs)
.claude/worktrees/
